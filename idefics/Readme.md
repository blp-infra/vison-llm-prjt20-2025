# Install idefics2 multi-modal text and image based

## System requirement minimum

### create new project directory

```
uv init projectname # new project directory
uv venv .venv # creates virtual environment
source .venv/bin/activate
uv lock
uv add transformers pillow
```